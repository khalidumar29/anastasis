// GENERATED ZONE: implement the migration per spec/product-spec.json.
// Read source files from import/, apply each field_map transform, insert rows.
// Idempotent: wipe tables before inserting. Log a count per table at the end.
// A row that fails a transform is logged with its source line number and
// skipped — never crash the whole migration.

throw new Error("migrate.ts not yet generated from spec/product-spec.json");
